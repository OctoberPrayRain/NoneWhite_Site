{
  "name": "nonewhite-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "npm --prefix server run lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
