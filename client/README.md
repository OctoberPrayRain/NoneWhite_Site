# NoneWhite_Site Client

Vue3 + Vite frontend for NoneWhite_Site.

## Scripts

```bash
npm install
npm run dev        # http://127.0.0.1:5173
npm run build
npm run preview
```

The Vite dev server proxies `/api` requests to the Rust backend on `http://localhost:3000`.
